import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const background = Color(0xFF1F2428);
  static const appBar = Color(0xFF101010);
  static const card = Color(0xFF2A2F33);

  // Text
  static const textPrimary = Colors.white;
  static const textMainColor = Color(0xB8FFFFFF);
  static const textSecondary = Color(0x69B0B0B0);

  // Accent
  static const primary = Colors.amber;

  // Bottom bar
  static const bottomBar = Color(0xDE29292F);
  static const bottomBarSelected = Color(0xFF1976D2);
}
